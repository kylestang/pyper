[package]
name = "pyper"
version = "0.1.0"
edition = "2024"
repository = "https://github.com/kylestang/pyper"
license = "ISC"
keywords = ["sigpipe", "signal", "bash", "pipe"]

[profile.release]
codegen-units = 1
lto = false
